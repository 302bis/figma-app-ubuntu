import { Packager } from "../packager";
export declare class RemoteBuilder {
    build(platform: string, targets: Array<string>, unpackedDirectory: string, packager: Packager, outDir: string): Promise<any>;
    private sendRequest(customHeaders, unpackedDirectory, packager, outDir);
}
